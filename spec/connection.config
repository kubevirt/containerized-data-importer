# For testing binary testing only.  Access credentials / url will be written into a k8s secret spec for controller pod
export BUCKET_ENDPOINT=
export BUCKET_ID=
export BUCKET_PWORD=

echo $BUCKET_ENDPOINT
echo $BUCKET_ID
echo $BUCKET_PWORD