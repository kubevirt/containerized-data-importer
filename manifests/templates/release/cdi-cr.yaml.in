apiVersion: cdi.kubevirt.io/v1beta1
kind: CDI
metadata:
  name: {{.CrName}}
spec:
  imagePullPolicy: {{.PullPolicy}}
  infra:
    nodeSelector:
      kubernetes.io/os: linux
  workload:
    nodeSelector:
      kubernetes.io/os: linux
